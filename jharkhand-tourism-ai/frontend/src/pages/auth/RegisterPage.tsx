import React from 'react';
import { Container, Typography } from '@mui/material';

const RegisterPage: React.FC = () => (
  <Container>
    <Typography variant="h2" sx={{ py: 4 }}>Register - Coming Soon</Typography>
  </Container>
);

export default RegisterPage;